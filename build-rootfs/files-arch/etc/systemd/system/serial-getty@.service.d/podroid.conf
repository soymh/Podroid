# Route the kernel-selected console tty through the podroid-getty wrapper,
# which picks the right device from the podroid.tty= cmdline marker (hvc0 on
# both backends today) and parks the other one. Applies to every
# serial-getty@ instance via the template drop-in.
[Service]
ExecStart=
ExecStart=/usr/local/bin/podroid-getty %I
